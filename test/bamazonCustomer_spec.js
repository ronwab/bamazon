const expect = require('chai').expect
var connection = require('../connection').connection;
const App = require('../bamazonCustomer')
describe('Test BamazonCustomer', () => {
    describe('test showAllDatafunction', () => {

        it('return data all data present in the database', () => {

        })

        it('should return a message if the table does not exist', () => {
         
            // let result = App.showAllData();
            expect(result).to.eql()
        })




    })
})